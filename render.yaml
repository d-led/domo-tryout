services:
  - type: web
    name: y-websocket-server
    rootDir: server
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 9870
      # WS_SECRET should be set in Render.com dashboard environment variables
      # It must match the GitHub secret WS_SECRET used for client builds
